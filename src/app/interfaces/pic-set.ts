/**
 * A collection of data URL strings of images taken with the camera.
 */
export interface PicSet {
    /**
     * Collection of data URL strings of images taken with the camera.
     */
    Pics: string[];
}